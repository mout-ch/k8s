---
# Traefik ForwardAuth Middleware for Authentik
# This middleware will require authentication through Authentik before accessing the Firefly Importer
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: firefly-importer-auth
spec:
  forwardAuth:
    address: http://ak-outpost-traefik-outpost.goauthentik.svc.cluster.local:9000/outpost.goauthentik.io/auth/traefik
    trustForwardHeader: true
    authResponseHeadersRegex: "^X-authentik-"
